// SiteFooter has been harmonized with Footer.tsx — both now render the
// same mobile-first, dark/gold design. Kept as a thin re-export so any
// existing `import { SiteFooter } from "@/components/SiteFooter"` still works.
export { SiteFooter, default } from "./Footer";
